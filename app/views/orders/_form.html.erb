
  <% if @product.quantity !=0 %>
  <% if user_signed_in? %>
   <%= form_for([@product, @product.orders.build]) do |f| %>
    <%= f.number_field :quantity %>
    <%= f.number_field :product_id %>
    <%= f.number_field :user_id %>
    <%= f.submit %>

<% end %>
<% else %>
<%= link_to 'login to purchase', new_user_session_path %>
<% end %>
<% end %>


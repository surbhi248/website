 <div class ="flickr_pagination mb-4">
<%= will_paginate @products, :container => false %>
  </div>s
<br>
<i>
<h1 style="color:violet;"> List of Products</h1>>
<h3>
<table bgcolor="pink">
  <thead>
    <tr style="color:blue">
      <th>Name</th>
      <th>Quantity</th>
      <th colspan="4"></th>
    </tr>
  </thead>

    <% @products.each do |product| %>
        <td><%= product.name %></td> 
          <% if product.quantity != 0 %>
            <p>Quantity:<%=product.quantity %></p>
          <% else %>
          <p>out of stock</p>
    <% end %>
    <% link_to 'View Details',product_path %>
    <% end %>
  <div class ="flickr_pagination mb-4">
<%= will_paginate @products, :container => false %>
  </div>
    



